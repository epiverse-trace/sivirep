---
title: "Reporte de Malaria, 2019"
output: html_document
date: "2022-09-19"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

library(dplyr)
library(tidyverse)
library(aweek)
rm(list=ls())
source("../R/funs.R")
```




```{r leer_seleccionar}
dat0 <- import_sivigila_data()
dat <- select_disease("MALAR", dat0)


```

## Distribución de casos por semana epdiemiológica


```{r inciencia, echo=TRUE}

plot_epiweek(dat, var_week = "SEMANA", var_cases = "conteo_casos", type = "week")
plot_epiweek(dat, var_week = "SEMANA", var_cases = "conteo_casos", type = "date")

```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
